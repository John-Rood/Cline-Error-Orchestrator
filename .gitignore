# User-specific configuration (contains local paths)
config/services.json

# Runtime data (local state files)
data/seen_errors.json
data/pending/*.json
data/last_poll.json
data/error_status.json

# Keep the directories but not their contents
!data/.gitkeep
!data/pending/.gitkeep

# Windows
Thumbs.db
Desktop.ini

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
